---
- name: Deploy NopCommarce Application
  become: yes
  hosts: webside
  tasks:
    - name: install dotnet and other package
      ansible.builtin.apt:
        name: "{{ item }}"
        state: present
        update_cache: yes
      loop:
        - dotnet
        - unzip


